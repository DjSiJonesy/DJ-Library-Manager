<UserControl
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Class="DJLibraryManager.UI.Controls.Workflow.WorkflowMediaLocationImportRow"
    x:Name="Root">

    <Border
        Padding="0"
        Margin="0,2">

        <Grid
            ColumnDefinitions="170,150,90,90,90,90,180,*"
            VerticalAlignment="Center">

            <!-- ===================================================== -->
            <!-- Main Path -->
            <!-- ===================================================== -->

            <TextBlock
                Grid.Column="0"
                VerticalAlignment="Center"
                FontSize="12"
                FontWeight="SemiBold"
                Text="{Binding MediaLocation.Path, ElementName=Root}" />

            <!-- ===================================================== -->
            <!-- Status -->
            <!-- ===================================================== -->

            <StackPanel
                Grid.Column="1"
                Orientation="Horizontal"
                VerticalAlignment="Center"
                Spacing="6">

                <Ellipse
                    Width="8"
                    Height="8"
                    Fill="{Binding MediaLocation.ImportStatusBrush,
                                   ElementName=Root}" />

                <TextBlock
                    FontSize="12"
                    Text="{Binding MediaLocation.ImportStatus,
                                   ElementName=Root}" />

            </StackPanel>

            <!-- ===================================================== -->
            <!-- Discovered -->
            <!-- ===================================================== -->

            <TextBlock
                Grid.Column="2"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                FontSize="12"
                Text="{Binding MediaLocation.DiscoveredFiles,
                               ElementName=Root,
                               StringFormat={}{0:N0}}" />

            <!-- ===================================================== -->
            <!-- Existing -->
            <!-- ===================================================== -->

            <TextBlock
                Grid.Column="3"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                FontSize="12"
                Text="{Binding MediaLocation.AlreadyInLibrary,
                               ElementName=Root,
                               StringFormat={}{0:N0}}" />

            <!-- ===================================================== -->
            <!-- Imported -->
            <!-- ===================================================== -->

            <TextBlock
                Grid.Column="4"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                FontSize="12"
                Text="{Binding MediaLocation.ImportedFiles,
                               ElementName=Root,
                               StringFormat={}{0:N0}}" />

            <!-- ===================================================== -->
            <!-- Failed -->
            <!-- ===================================================== -->

            <TextBlock
                Grid.Column="5"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                FontSize="12"
                Text="{Binding MediaLocation.FailedFiles,
                               ElementName=Root,
                               StringFormat={}{0:N0}}" />   

            <!-- ===================================================== -->
            <!-- Last Imported -->
            <!-- ===================================================== -->

            <TextBlock
                Grid.Column="6"
                Margin="12,0,0,0"
                VerticalAlignment="Center"
                FontSize="12"
                Text="{Binding MediaLocation.LastImported,
                               ElementName=Root,
                               TargetNullValue=Never}" />

            <!-- ===================================================== -->
            <!-- Action -->
            <!-- ===================================================== -->

            <Button
                Grid.Column="7"
                Width="90"
                HorizontalAlignment="Center"
                Theme="{StaticResource DJLM.SmallPrimaryButton}"
                Command="{Binding ImportCommand, ElementName=Root}"
                CommandParameter="{Binding MediaLocation, ElementName=Root}"
                IsEnabled="{Binding MediaLocation.CanImport,
                                    ElementName=Root}"
                Content="{Binding MediaLocation.ImportActionText,
                                  ElementName=Root}" />

        </Grid>

    </Border>


</UserControl>